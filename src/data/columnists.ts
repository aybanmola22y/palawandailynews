/**
 * Legacy demo columnists — kept empty on purpose.
 * Real public author profiles are managed under Admin → Staff (Supabase).
 * Do not re-add hardcoded dummy names here; they used to reappear after Remove.
 */
export const columnists: {
  id: string;
  name: string;
  role: string;
  bio: string;
  image: string;
}[] = [];
